<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.ricky.boardserver.mapper.PostMapper">
    <insert id="register" parameterType="me.ricky.boardserver.dto.PostDTO">
        INSERT INTO post ( name, isAdmin, contents, createTime, views, categoryId, userId, fileId, updateTime)
        VALUES (#{name}, #{isAdmin}, #{contents}, #{createTime}, #{views}, #{categoryId}, #{userId}, #{fileId}, #{updateTime})
    </insert>
    <select id="selectMyPosts" resultType="me.ricky.boardserver.dto.PostDTO">
        SELECT *
        FROM post
        WHERE userId = #{userId}
        ORDER BY createTime DESC
    </select>
    <update id="update" parameterType="me.ricky.boardserver.dto.PostDTO">
        UPDATE post
        SET name        = #{name},
            contents    = #{contents},
            views       = #{views},
            categoryId  = #{categoryId},
            userId      = #{userId},
            fileId      = #{fileId},
            updateTime = #{updateTime}
        WHERE id = #{id}
    </update>
    <delete id="delete">
        DELETE
        FROM post
        WHERE id = #{postId}
    </delete>

</mapper>